# .github/workflows/new-release.yml
name: Release

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  new_release:
    name: release ${{ matrix.build }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        build: [linux, windows, mac]
        include:
          - build: linux
            target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            binary: mqtt_test
          - build: windows
            target: x86_64-pc-windows-msvc
            os: windows-latest
            binary: mqtt_test.exe
          - build: mac
            target: x86_64-apple-darwin
            os: macos-latest
            binary: mqtt_test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Install musl-tools on Linux
        if: matrix.build == 'linux'
        run: sudo apt-get update --yes && sudo apt-get install --yes musl-tools
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}
      - name: Build artifacts
        run: cargo build --locked --release --target ${{ matrix.target }}
      - name: Package artifacts (Non-Windows)
        if: matrix.build != 'windows'
        run: |
          RELEASE_DIR=target/${{ matrix.target }}/release
          cp README.md LICENSE REQUIREMENTS_TABLE.md "${RELEASE_DIR}/"
          cd "${RELEASE_DIR}"
          ARCHIVE=mqtt_test_${{ matrix.target }}.zip
          zip -9r ${ARCHIVE} ${{ matrix.binary }} README.md LICENSE REQUIREMENTS_TABLE.md
          printf "%s %s" "$(sha256sum "${ARCHIVE}" | cut -d ' ' -f 1)" "$ARCHIVE" > "${ARCHIVE}.sha256sum"
      - name: Package artifacts (Windows)
        if: matrix.build == 'windows'
        shell: pwsh
        run: |
          $RELEASE_DIR = "target/${{ matrix.target }}/release"
          Copy-Item README.md, LICENSE, REQUIREMENTS_TABLE.md -Destination $RELEASE_DIR
          cd $RELEASE_DIR
          $ARCHIVE = "mqtt_test_${{ matrix.target }}.zip"
          Compress-Archive -Path ${{ matrix.binary }}, README.md, LICENSE, REQUIREMENTS_TABLE.md -DestinationPath $ARCHIVE -CompressionLevel Optimal
          $hash = (Get-FileHash $ARCHIVE -Algorithm SHA256).Hash.ToLower()
          "$hash $ARCHIVE" | Out-File -Encoding ascii -NoNewline "$ARCHIVE.sha256sum"
      - name: Upload release assets (release)
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            target/${{ matrix.target }}/release/mqtt_test_${{ matrix.target }}.zip
            target/${{ matrix.target }}/release/mqtt_test_${{ matrix.target }}.zip.sha256sum
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (manual)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: mqtt_test_${{ matrix.build }}
          path: |
            target/${{ matrix.target }}/release/mqtt_test_${{ matrix.target }}.zip
            target/${{ matrix.target }}/release/mqtt_test_${{ matrix.target }}.zip.sha256sum
